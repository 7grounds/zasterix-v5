# ─────────────────────────────────────────────────────────────────
# Zasterix-V5 — Environment Variable Reference
# Copy this file to `.env` and fill in your real values.
# NEVER commit `.env` to version control.
# ─────────────────────────────────────────────────────────────────

# ── Supabase ──────────────────────────────────────────────────────
# Project URL (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co

# Anon/public key — safe to expose to the browser (Row Level Security enforced)
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-supabase-anon-key>

# Service-role key — NEVER expose to the browser; engine/server use only
SUPABASE_SERVICE_ROLE_KEY=<your-supabase-service-role-key>

# Convenience alias used by the engine process
SUPABASE_URL=https://<project-ref>.supabase.co

# ── AI / LLM ──────────────────────────────────────────────────────
# Google Gemini API key (used by agents in the engine to generate code)
GEMINI_API_KEY=<your-gemini-api-key>

# ── Engine (Railway / Codespaces) ─────────────────────────────────
# How often (ms) the engine polls Supabase for pending tasks (default: 5000)
ENGINE_POLL_INTERVAL_MS=5000

# ── GitHub Integration ────────────────────────────────────────────
# Personal Access Token (or GitHub App token) with `contents: write` permission
# Used by engine/src/git.ts to commit agent-generated code back to the repo
GITHUB_TOKEN=<your-github-token>
GITHUB_REPO_OWNER=7grounds
GITHUB_REPO_NAME=zasterix-v5
GITHUB_BRANCH=main

# Node environment
NODE_ENV=development
